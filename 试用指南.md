# 试用指南(本周可用)

三个档位,按需取用:

| 档位 | 能干什么 | 现在状态 |
|---|---|---|
| **① 体验版(网页,零安装)** | 随便点随便试,看全流程长什么样;刷新即重置,**不能记真账** | ✅ 已可用 |
| **② 单机试用版(店里电脑)** | **记真账**:数据存在本机文件里,重启不丢,天天自动备份 | ✅ 已可用 |
| **③ 云端版(手机随时看)** | 部署到 Cloudflare,任何设备打开,数据在云端 | ⏳ 等 Cloudflare 账号授权,拿到后半天内上线 |

## ① 体验版:马上打开

浏览器打开(链接由会话提供,claude.ai Artifact 页面),四个页签都能玩:
首页「今日总览」点【听汇报】听一遍;「今晚要办」点【填入示例消息】走
粘贴→拆单→确认;「记账台」单价已按上回预填,填实称一键挂账;「客户的账」
看三色账龄和客户 360。**数据在页面内存里,刷新即归零**——放心乱点。

## ② 单机试用版:店里电脑记真账

**装一次,以后双击就开:**

1. 装 Node.js LTS(https://nodejs.org/zh-cn ,一路下一步);
2. 下载本仓库代码到电脑(GitHub 页面绿色 Code 按钮 → Download ZIP,解压);
3. 进 `app/server/` 文件夹,双击 **`启动工作台.bat`**——浏览器自动打开工作台。

- 关掉黑窗口 = 停服务;要用再双击,**账都在**;
- 想在手机上开(同一 WiFi):改双击 **`启动工作台-手机可开.bat`**,设个口令,
  手机浏览器开 `http://电脑IP:8788`,输一次口令记住一个月;
- Mac/Linux:`app/server/start.sh`。

**数据在哪、怎么不丢:**

- 账本就是一个文件:`app/server/data/ledger.sqlite`;
- 每天第一次启动自动把它复制进 `data/backups/`(按日期存);
- 换电脑 = 拷走 `data/` 文件夹;彻底重来 = 删掉 `data/` 文件夹。

**试用第一周该做的两件事:**

1. **校对档案**:客户和商品档案来自店里照片识读,存疑处(带 ? 的)在
   工作台里改一遍(design/14 有底账);
2. **不用录价格表**:商品档案价可以先空着——每记一笔账,系统就记住这家
   这个货的价,第二天记账台自动按"上回价"预填。

## ③ 云端版:需要一样东西

代码本身已按 Cloudflare Workers + D1 写好(`app/server/wrangler.toml`)。
需要一个 Cloudflare 账号的 API Token(免费套餐够用)。提供之后:建库、
跑 schema、部署、加口令,半天内给出正式网址。

## 试用时最想听的反馈

- 拆单认错了哪句话(原话发来,进语料);
- 哪个按钮的字看不懂、哪一步觉得多余;
- 【听汇报】念的顺序和口气对不对味。
